# Polymarket Live Monitor - Environment Variables

# =============================================================================
# WebSocket Configuration
# =============================================================================

# Polymarket Real-Time WebSocket URL
# Default: wss://ws-live-data.polymarket.com
NEXT_PUBLIC_POLYMARKET_REALTIME_WS_URL=wss://ws-live-data.polymarket.com

# Legacy fallback (deprecated, will be removed)
# Default: wss://clob.polymarket.com/ws
NEXT_PUBLIC_POLYMARKET_WS_URL=wss://clob.polymarket.com/ws

# WebSocket Connection Timeout (milliseconds)
# Default: 10000 (10 seconds)
NEXT_PUBLIC_WS_TIMEOUT=10000

# WebSocket Max Reconnection Attempts
# Default: 5
NEXT_PUBLIC_WS_MAX_RECONNECT=5

# =============================================================================
# CLOB API Configuration (Optional - for authenticated features)
# =============================================================================

# CLOB API Base URL
# Default: https://clob.polymarket.com
NEXT_PUBLIC_CLOB_API_URL=https://clob.polymarket.com

# Chain ID for Polygon
# 137 = Polygon Mainnet, 80001 = Mumbai Testnet
NEXT_PUBLIC_CHAIN_ID=137

# =============================================================================
# Gamma API Configuration
# =============================================================================

# Gamma API Base URL (markets, events, comments)
# Default: https://gamma-api.polymarket.com
NEXT_PUBLIC_GAMMA_API_URL=https://gamma-api.polymarket.com

# =============================================================================
# Feature Flags
# =============================================================================

# Enable/Disable Mock Data Fallback
# true = Use mock data when WebSocket fails
# false = Show error only, no fallback
NEXT_PUBLIC_ENABLE_MOCK_FALLBACK=true

# Enable/Disable Debug Logging
# true = Verbose console logs
# false = Production logging only
NEXT_PUBLIC_DEBUG_MODE=false

# =============================================================================
# IMPORTANT SECURITY NOTES
# =============================================================================
#
# 1. NEVER commit actual API keys or secrets to version control
# 2. For authenticated CLOB features, use server-side environment variables
#    (without NEXT_PUBLIC_ prefix) and expose only through secure API routes
# 3. This .env.example file should only contain placeholder values
# 4. Copy this file to .env.local and update with your actual values
#
# =============================================================================
